name: ctb
channels:
  - conda-forge
dependencies:
  - python=3.11
  - fastapi=0.114
  - uvicorn=0.30
  - pytest=8.3
  - pytest-cov=5.0
  - pydantic=2.8
  - httpx>=0.27
  - anyio>=4.0
  - pyyaml>=6.0
  - pip
  # If you later need extras not on conda-forge, add them under pip:
  # - pip:
  #     - some-package-only-on-pip
